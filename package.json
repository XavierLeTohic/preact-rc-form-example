{
  "private": false,
  "name": "preact-rc-form-example",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-synacor": "^3.0.4",
    "if-env": "^1.0.0",
    "preact-cli": "^2.2.1"
  },
  "dependencies": {
    "flexboxgrid": "^6.3.1",
    "preact": "^8.2.6",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.7"
  }
}
